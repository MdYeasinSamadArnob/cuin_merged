# ============================================
# Docker-image dependency set for CUIN v2 backend
# ============================================
# Generated from requirements.txt with the Spark/Splink-only packages
# removed: pyspark, splink, py4j, scikit-learn, scipy, igraph, altair,
# oracledb, Faker. Confirmed via `grep` across every .py file outside
# engine/spark_er/ that none of these are imported by the live API or
# the DuckDB pipeline (pipeline.duckdb_orchestrator, api/*, engine/scoring,
# engine/normalize, engine/blocking, engine/clustering) -- they are only
# reachable through pipeline.spark_orchestrator.SparkPipelineOrchestrator,
# which routes_datasource.py now imports lazily (only if `engine=spark`
# is explicitly requested) specifically so this trimmed set is enough to
# boot the whole app. Regenerate by re-running the same exclusion filter
# against requirements.txt if that file changes; do not hand-edit versions
# here independently of it.
annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.12.1
asyncpg==0.31.0
attrs==25.4.0
certifi==2026.2.25
cffi==2.0.0
click==8.3.1
cryptography==46.0.5
duckdb==1.5.0
fastapi==0.135.1
h11==0.16.0
httpcore==1.0.9
httptools==0.7.1
httpx==0.28.1
idna==3.11
Jinja2==3.1.6
joblib==1.5.3
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
MarkupSafe==3.0.3
narwhals==2.18.0
neo4j==6.1.0
numpy==2.2.6
packaging==26.0
pandas==2.3.3
pyarrow==23.0.1
psycopg2-binary==2.9.11
PyMySQL==1.1.2
pycparser==3.0
pydantic==2.12.5
pydantic-settings==2.13.1
pydantic_core==2.41.5
python-dateutil==2.9.0.post0
python-dotenv==1.2.2
python-multipart==0.0.22
pytz==2026.1.post1
PyYAML==6.0.3
redis==7.3.0
referencing==0.37.0
rpds-py==0.30.0
six==1.17.0
sqlglot==29.0.1
starlette==0.52.1
texttable==1.7.0
threadpoolctl==3.6.0
tqdm==4.67.3
typing-inspection==0.4.2
typing_extensions==4.15.0
tzdata==2025.3
uvicorn==0.41.0
uvloop==0.22.1
watchfiles==1.1.1
websockets==16.0
